---
jupytext:
  formats: md:myst
  text_representation:
    extension: .md
    format_name: myst
kernelspec:
  display_name: Python 3
  language: python
  name: python3
---

# 🏆Selbsttest: V2

````{admonition} Hinweis
:class: hinweis
Diese Übungsaufgaben dienen Ihrer Selbsteinschätzung und helfen Ihnen, das im Kapitel Gelernte zu reflektieren.

Sie können die Fragen in beliebiger Reihenfolge bearbeiten und die Beantwortung auch mehrfach versuchen. 

**So funktioniert es:**
- Wählen Sie bei jeder Frage die Antwort(en) aus, die Sie für richtig halten
- Lesen Sie das Feedback zu den einzelnen Antwortoptionen sorgfältig durch
- Die Erklärungen helfen Ihnen, Ihr Verständnis zu vertiefen – auch bei korrekten Antworten 

Es erfolgt keine Bewertung oder Speicherung Ihrer Ergebnisse. Nutzen Sie dieses Assessment, um Wissenslücken zu identifizieren und gegebenenfalls die entsprechenden Abschnitte des Kapitels nochmals zu bearbeiten. 

**Geschätzte Zeit**: XX

Viel Erfolg!
````

# Aufgabe 0 multiple boxes

```{raw} html
<!-- ░░░  SHARED CSS  ░░░ -->
<style>
.dnd-ex      {display:flex;flex-direction:column;gap:1.3rem;margin-bottom:2.2rem}
.dnd-item    {background:#eef2ff;border:1px solid #4f46e5;border-radius:.6rem;
              padding:.4rem .8rem;margin:.25rem;font-weight:500;cursor:move;
              box-shadow:0 1px 2px rgb(0 0 0 / .06);transition:transform .15s,box-shadow .15s}
.dnd-item:active{transform:scale(.92);box-shadow:0 4px 6px rgb(0 0 0 / .15)}
.dnd-box     {min-height:3rem;min-width:8rem;padding:.55rem .7rem;border:2px dashed #9ca3af;
              border-radius:.8rem;display:flex;flex-wrap:wrap;gap:.4rem;align-items:center;
              transition:border-color .2s,background .2s}
.dnd-box.dragover{background:#fefce8;border-color:#4f46e5}
.dnd-box.ok      {background:#f0fdf4;border-color:#16a34a}
.dnd-btn     {background:#4f46e5;color:#fff;border:0;border-radius:.55rem;
              padding:.45rem 1.2rem;margin-right:.7rem;font-weight:500;cursor:pointer;
              transition:background .2s}
.dnd-btn:hover   {background:#4338ca}
.arrow       {font-size:1.6rem;color:#9ca3af;user-select:none;margin:0 .4rem}
</style>

<!-- ─────────────  EXAMPLE 1 — “Choose the primes”  ───────────── -->
<div class="dnd-ex" data-answer="2,5" data-order="false">
  <p><strong>Task A – choose all prime numbers</strong></p>

  <div class="dnd-box choices vertical">
    <span class="dnd-item" draggable="true" data-key="2">2</span>
    <span class="dnd-item" draggable="true" data-key="9">9</span>
    <span class="dnd-item" draggable="true" data-key="4">4</span>
    <span class="dnd-item" draggable="true" data-key="5">5</span>
  </div>

  <div class="dnd-box target"></div>

  <div>
    <button class="dnd-btn check">Check</button>
    <button class="dnd-btn reset">Reset</button>
  </div>
</div>

<!-- ─────────────  EXAMPLE 2 — “Order 2 → 4 → 3”  ───────────── -->
<div class="dnd-ex" data-answer="2,4,3" data-order="true">
  <p><strong>Task B – arrange the numbers in order 2 → 4 → 3</strong></p>

  <div class="dnd-box choices">
    <span class="dnd-item" draggable="true" data-key="3">3</span>
    <span class="dnd-item" draggable="true" data-key="2">2</span>
    <span class="dnd-item" draggable="true" data-key="4">4</span>
  </div>

  <!-- leave this empty; script will inject slots + arrows -->
  <div class="slots"></div>

  <div>
    <button class="dnd-btn check">Check</button>
    <button class="dnd-btn reset">Reset</button>
  </div>
</div>

<!-- ░░░  UNIVERSAL JS  ░░░ -->
<script>
document.addEventListener('DOMContentLoaded', () => {

  /* ═════════  INITIALISE EVERY .dnd-ex  ═════════ */
  document.querySelectorAll('.dnd-ex').forEach(ex=>{
      const ordered   = ex.dataset.order === 'true';
      const answerArr = ex.dataset.answer.split(',');            // ["2","5"] or ["2","4","3"]
      const choices   = ex.querySelector('.choices');
      const resetBtn  = ex.querySelector('.reset');
      const checkBtn  = ex.querySelector('.check');
      let dragging = null;

      /* ── build slots w/ arrows for ordering tasks ── */
      let targets;
      if (ordered){
        const slotsWrap = ex.querySelector('.slots');
        for (let i=0;i<answerArr.length;i++){
          const slot=document.createElement('div');
          slot.className='dnd-box slot'; slot.dataset.pos=i;
          slotsWrap.appendChild(slot);
          if (i<answerArr.length-1){
            const arrow=document.createElement('span');
            arrow.className='arrow'; arrow.innerHTML='&rarr;';
            slotsWrap.appendChild(arrow);
          }
        }
        targets = ex.querySelectorAll('.slot');
      }else{
        targets = [ ex.querySelector('.target') ];               // single target box
      }

      /* ── make cards draggable ── */
      ex.querySelectorAll('.dnd-item').forEach(card=>{
         card.addEventListener('dragstart', () => dragging = card);
      });

      /* ── choices box (unlimited drops) ── */
      makeDroppable(choices,false);

      /* ── every target box ── */
      targets.forEach(t => makeDroppable(t, ordered));           // single-card if ordered task

      function makeDroppable(box, single){
        box.addEventListener('dragover',e=>{
           if (!single || box.children.length===0){ e.preventDefault(); box.classList.add('dragover'); }
        });
        box.addEventListener('dragleave',()=>box.classList.remove('dragover'));
        box.addEventListener('drop',e=>{
           e.preventDefault(); box.classList.remove('dragover');
           if (!single || box.children.length===0) box.appendChild(dragging);
        });
      }

      /* ── RESET ── */
      resetBtn.addEventListener('click', reset);

      function reset(){
        targets.forEach(t=>{
          while(t.firstChild) choices.appendChild(t.firstChild);
          t.classList.remove('ok');
        });
      }

      /* ── CHECK ── */
      checkBtn.addEventListener('click', ()=>{
        let ok;
        if (ordered){
          const picked=[...targets].map(t=>t.firstElementChild?.dataset.key||'');
          ok = picked.join() === answerArr.join();
        }else{
          const picked=[...targets[0].querySelectorAll('.dnd-item')].map(x=>x.dataset.key).sort();
          ok = picked.length===answerArr.length &&
               picked.join() === answerArr.slice().sort().join();
        }

        if (ok){
          alert('✅ Correct!');
          targets.forEach(t=>t.classList.add('ok'));
        }else{
          alert('❌ Not correct.');
          reset();                                              // auto-reset on wrong attempt
        }
      });
  });
});
</script>
```

